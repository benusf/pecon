Installation
============

Install from PyPI:

.. code-block:: bash

   pip install pecon

Development (Editable Install):

.. code-block:: bash

   git clone git@github.com:pecon-devs/pecon.git
   cd pecon
   pip install -e .
